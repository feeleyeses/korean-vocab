name: Mirror Korean Word Field (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Migration status
        run: |
          echo 'The chatgpt.site migration is complete.'
          echo 'This workflow is intentionally retired so it cannot overwrite GitHub-native changes.'
          echo 'Use Deploy Korean Word Field for production deployments.'
