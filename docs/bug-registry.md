# Bug Registry

Every bug fix must add or update a registry item before commit.

| ID | Status | Area | Summary | Guardrail |
| --- | --- | --- | --- | --- |
| BUG-2026-08-29-001 | Mitigated | Layout runtime | Multiple historical CSS/runtime files could compete over study/review/learned-card geometry after React redraws. | `assets/layout-system-v1a.css` and `assets/layout-system-v1a.js` are the only page-mounted Sprint 1 layout authority; tests assert `data-kwf-layout-authority="layout-system"`. |
| BUG-2026-08-29-002 | Guarded | Vocabulary data | Future duplicated headwords could be merged as false polysemy if identity is based only on display text. | `docs/vocabulary-model.md` requires `lexicalEntryId` ownership and sense-level IDs; `scripts/vocab-quality-audit.mjs` blocks invalid draft entries. |
| BUG-2026-08-29-003 | Fixed | DOM audit | Legacy `kwf-v2-*`/`kwf-v3-*` layout classes could remain in the DOM and obscure which system owns layout. | Historical `ui-v3*`, `study-card-v2*`, `layout-v2*`, and `stabilize.css` assets were deleted; active DOM uses `kwf-layout-review-stack` from the Layout System. |
| BUG-2026-08-29-004 | Fixed | Study card geometry | Initial learning buttons were not guarded as a centered group and the revealed Continue button could drift onto a different action rail. | `scripts/ui-regression.mjs` checks 1920 and 390 viewports for shared centerline, fixed Continue width, and hover-stable button geometry. |
| BUG-2026-08-29-005 | Fixed | Hover states | Button hover rules could animate or modify geometry-sensitive properties, causing visual flicker or layout shift. | Layout System restricts transitions to color, border color, shadow, and opacity; regression checks width, height, padding, margin, border, font weight, and transform before and after hover. |
| BUG-2026-08-29-006 | Fixed | Button system | Revealed study/review Continue buttons could render shorter than the initial action buttons because the runtime still carried an old `180px` shortcut path. | `--kwf-primary-action-w` is the single source for initial Primary CTA and Continue width; `scripts/ui-regression.mjs` compares Continue width directly against the initial button token. |
| BUG-2026-08-29-007 | Guarded | Hero copy | Desktop memory-profile and learned-library copy could wrap unexpectedly at 1920px, increasing perceived page height. | Desktop Layout System applies nowrap only at wide viewports, and `scripts/ui-regression.mjs` asserts the three required strings render as one line. |
| BUG-2026-08-29-008 | Guarded | UI audit | Site buttons could pass card-specific tests while still allowing hover jitter or transition-all regressions elsewhere. | `scripts/ui-consistency-audit.mjs` audits visible controls in Desktop 1920 and Mobile 390 for stable hover geometry, fixed box metrics, transition properties, and CLS. |
| BUG-2026-08-29-009 | Fixed | UI polish | Memory Hero text, learned-library filters, learned rows, and Continue CTA could still use mismatched alignment sources after the first layout cleanup. | `assets/layout-system-v1a.css` and `assets/layout-system-v1a.js` now share tokens for Hero inset, Primary CTA dimensions, filter title/chip baseline, and learned-card vertical alignment. |
| BUG-2026-08-29-010 | Fixed | Layout lock | Continue used the single-button width instead of the full action group width, and expanding the learned library widened the review container. | `--kwf-primary-action-group-w`, `--kwf-hero-bottom-gap`, and `--kwf-review-container-w` lock the action rail, Hero-to-card spacing, and review container width before and after expansion. |
| BUG-2026-08-29-011 | Fixed | Content Safe Area | Study, polysemy, review, learned-library, and expanded drawers could resolve different first-level widths and visual rhythm. | `--kwf-content-safe-w`, `--kwf-page-pad-x`, `--kwf-section-gap`, `--kwf-card-gap`, `--kwf-card-pad`, and `--kwf-footer-pad` are the shared layout tokens for first-level modules and drawer footers, including `#learned-library-section`. |
| BUG-2026-08-29-012 | Fixed | Visual rhythm | Page rhythm remained loose and polysemy/learned controls had uneven hierarchy, while learned-row next-review text could drift within the status cluster. | Global spacing tokens now use a denser scale; polysemy controls and learned filters share baseline button rules; learned status uses a fixed three-row template for favorite, state, and next review. |
| BUG-2026-08-29-013 | Fixed | Learned library layout | Learned word rows could place favorite, memory state, and next-review time differently when status text or row content changed, and pager controls could sit off-baseline. | `#learned-library-section` now uses a fixed-width status grid and a flex-centered pager; browser validation checks mixed statuses for equal row height and identical relative right-column positions. |
| BUG-2026-08-29-014 | Fixed | Learned library favorite | The favorite star glyph could sit visually high inside the circular learned-row button because text baseline alignment overrode the circular control center. | Learned-row favorite chips use grid centering and runtime geometry normalization so the star stays centered without changing size, color, or spacing tokens. |
