# Bug Registry

Every bug fix must add or update a registry item before commit.

| ID | Status | Area | Summary | Guardrail |
| --- | --- | --- | --- | --- |
| BUG-2026-08-29-001 | Mitigated | Layout runtime | Multiple historical CSS/runtime files could compete over study/review/learned-card geometry after React redraws. | `assets/layout-system-v1a.css` and `assets/layout-system-v1a.js` are the only page-mounted Sprint 1 layout authority; tests assert `data-kwf-layout-authority="layout-system"`. |
| BUG-2026-08-29-002 | Guarded | Vocabulary data | Future duplicated headwords could be merged as false polysemy if identity is based only on display text. | `docs/vocabulary-model.md` requires `lexicalEntryId` ownership and sense-level IDs; `scripts/vocab-quality-audit.mjs` blocks invalid draft entries. |
| BUG-2026-08-29-003 | Fixed | DOM audit | Legacy `kwf-v2-*`/`kwf-v3-*` layout classes could remain in the DOM and obscure which system owns layout. | Historical `ui-v3*`, `study-card-v2*`, `layout-v2*`, and `stabilize.css` assets were deleted; active DOM uses `kwf-layout-review-stack` from the Layout System. |
| BUG-2026-08-29-004 | Fixed | Study card geometry | Initial learning buttons were not guarded as a centered group and the revealed Continue button could drift onto a different action rail. | `scripts/ui-regression.mjs` checks 1920 and 390 viewports for shared centerline, fixed Continue width, and hover-stable button geometry. |
| BUG-2026-08-29-005 | Fixed | Hover states | Button hover rules could animate or modify geometry-sensitive properties, causing visual flicker or layout shift. | Layout System restricts transitions to color, border color, shadow, and opacity; regression checks width, height, padding, margin, border, font weight, and transform before and after hover. |
| BUG-2026-08-29-006 | Fixed | Button system | Revealed study/review Continue buttons could render shorter than the initial action buttons because the runtime still carried an old `180px` shortcut path. | `--kwf-primary-action-w` is the single source for initial Primary CTA and Continue width; `scripts/ui-regression.mjs` compares Continue width directly against the initial button token. |
| BUG-2026-08-29-007 | Guarded | Hero copy | Desktop memory-profile and learned-library copy could wrap unexpectedly at 1920px, increasing perceived page height. | Desktop Layout System applies nowrap only at wide viewports, and `scripts/ui-regression.mjs` asserts the three required strings render as one line. |
| BUG-2026-08-29-008 | Guarded | UI audit | Site buttons could pass card-specific tests while still allowing hover jitter or transition-all regressions elsewhere. | `scripts/ui-consistency-audit.mjs` audits visible controls in Desktop 1920 and Mobile 390 for stable hover geometry, fixed box metrics, transition properties, and CLS. |
| BUG-2026-08-29-009 | Fixed | UI polish | Memory Hero text, learned-library filters, learned rows, and Continue CTA could still use mismatched alignment sources after the first layout cleanup. | `assets/layout-system-v1a.css` and `assets/layout-system-v1a.js` now share tokens for Hero inset, Primary CTA dimensions, filter title/chip baseline, and learned-card vertical alignment. |
| BUG-2026-08-29-010 | Fixed | Layout lock | Continue used the single-button width instead of the full action group width, and expanding the learned library widened the review container. | `--kwf-primary-action-group-w`, `--kwf-hero-bottom-gap`, and `--kwf-review-container-w` lock the action rail, Hero-to-card spacing, and review container width before and after expansion. |
| BUG-2026-08-29-011 | Fixed | Content Safe Area | Study, polysemy, review, learned-library, and expanded drawers could resolve different first-level widths and visual rhythm. | `--kwf-content-safe-w`, `--kwf-page-pad-x`, `--kwf-section-gap`, `--kwf-card-gap`, `--kwf-card-pad`, and `--kwf-footer-pad` are the shared layout tokens for first-level modules and drawer footers, including `#learned-library-section`. |
| BUG-2026-08-29-012 | Fixed | Visual rhythm | Page rhythm remained loose and polysemy/learned controls had uneven hierarchy, while learned-row next-review text could drift within the status cluster. | Global spacing tokens now use a denser scale; polysemy controls and learned filters share baseline button rules; learned status uses a fixed three-row template for favorite, state, and next review. |
| BUG-2026-08-29-013 | Fixed | Learned library layout | Learned word rows could place favorite, memory state, and next-review time differently when status text or row content changed, and pager controls could sit off-baseline. | `#learned-library-section` now uses a fixed-width status grid and a flex-centered pager; browser validation checks mixed statuses for equal row height and identical relative right-column positions. |
| BUG-2026-08-29-014 | Fixed | Learned library favorite | The favorite star glyph could sit visually high inside the circular learned-row button because text baseline alignment overrode the circular control center. | Learned-row favorite chips use grid centering and runtime geometry normalization so the star stays centered without changing size, color, or spacing tokens. |
| BUG-2026-08-29-015 | Fixed | Learned library Right Info Group | The favorite circle itself could sit above the memory-state control because the right column used a three-row stack instead of a compact grouped layout. | The learned row status column now uses a two-row Right Info Group: favorite plus memory state on the same centerline, with next-review time centered directly below across mixed favorite and memory states. |
| BUG-2026-08-30-001 | Fixed | Reveal card runtime | The rebuilt Reveal Card moved React-owned Continue/status/map nodes into the runtime shell, which could detach nodes during React commits, make Continue appear stuck, and leave stale reveal content after TOPIK or card changes. | `kwf-reveal-v2` now clones display-only content, uses a proxy Continue button that delegates to the original React button, keeps original nodes in place, and removes the shell when the card returns to initial state. |
| BUG-2026-08-30-002 | Fixed | Reveal card geometry | The revealed card could shrink below the initial learning card and the green progress indicator could render as a separate floating element outside the card boundary. | The revealed card now uses the same `--kwf-card-h` as the initial card and renders an attached top progress strip inside the `kwf-reveal-v2` shell with matching card width and top radius. |
| BUG-2026-08-30-003 | Fixed | Study card structure | Initial learning actions and revealed Continue could disappear, duplicate, or inherit old absolute inline styles because the visible card shell mixed cloned controls with React-owned controls. | `kwf-card-v3` renders one visible shell for initial and revealed states, keeps original React nodes hidden with pointer events disabled, and delegates user actions through clean proxy buttons. |
| BUG-2026-08-30-004 | Fixed | Review card structure | Review-card multiple-choice options were not rendered by the shared visible card shell, and proxy buttons could look like plain text without interactive affordance. | `kwf-card-v3` now renders review options through the same Footer region and applies shared primary, secondary, option, hover, pressed, cursor, and focus-visible button states. |
| BUG-2026-08-30-005 | Fixed | Review reveal drawer | Opening the option-Korean drawer on sound-rule review cards could inherit the deprecated absolute `answer-map` overlay, clipping the drawer and hiding Continue. | `kwf-card-v3-answer-map` is now a bounded internal flow region with its own scrollable option list, while the v3 Footer keeps Continue visible, rounded, and clickable. |
| BUG-2026-08-30-006 | Fixed | Review reveal drawer | The bounded option-Korean drawer still used internal scrolling and could show incomplete options when sound-rule review content was present. | Review reveal cards with an option drawer now use a compact no-scroll 2x2 grid, preserve fixed 500px card height, and keep Continue fixed at the bottom without overlap. |
| BUG-2026-08-30-007 | Fixed | Card footer layout | Revealed learning/review cards could let sound rules or the option-Korean drawer compete with the Continue action rail, and header metadata could visually overpower the headword. | `kwf-card-v3` now uses explicit Header / ContentArea / Footer grid rows, a fixed 104px Footer with shared Continue metrics, a no-scroll compact drawer inside ContentArea, and caption-level header typography. |
| BUG-2026-08-30-008 | Fixed | Review card content area | Review initial cards could show a stray divider below options, while closed option-Korean controls reserved too much height and opened drawers pushed content instead of overlaying above Continue. | The v3 answer map now keeps a 34px collapsed row, opens its 2x2 option map as an upward overlay with no list scrollbar, and leaves the fixed Continue Footer untouched. |
| BUG-2026-08-30-009 | Fixed | Learning flow | Completing a learning group could scroll the page down toward the polysemy section even though the user was still in the learning workflow. | The v3 Continue proxy now detects post-completion drift into the polysemy viewport and restores the page to the `#study` section without changing answer recording. |
| BUG-2026-08-30-010 | Fixed | Card flow | Sound-rule revealed cards could shrink Hero typography relative to the initial card, and the completed learning summary could render above the learning controls instead of in the study-card slot. | The v3 card keeps shared Hero/meta typography across initial and revealed states, uses non-overlapping content measurements in validation, and gives `.session-summary` the same safe-area/order as `.study-grid`. |
| BUG-2026-09-01-001 | Fixed | Vocabulary pipeline | Published collocation meanings could silently fallback to `sense.gloss`, causing phrase rows like verb phrases to display the headword meaning as if it were a verified collocation translation. | `scripts/vocab-pipeline.mjs` now publishes `collocation.zh` only from raw `collocationZh`; `scripts/vocab-quality-audit.mjs` blocks non-KRDict fallback-to-gloss rows and reports collocation zh coverage by TOPIK level. |
| BUG-2026-09-01-002 | Guarded | Vocabulary expansion | Raw expansion drafts could accidentally enter the production learning queue before human review, increasing vocabulary quantity while lowering published quality. | `scripts/vocab-pipeline.mjs` publishes only `approved` entries, records skipped drafts, and `scripts/vocab-quality-audit.mjs --raw=data/vocabulary.raw.json` reports draft/needs_review counts, required-field gaps, duplicate IDs, and duplicate headwords. |
| BUG-2026-09-01-003 | Fixed | Card interaction | The visible proxy Continue button could dispatch mouse events without triggering the current React-owned Continue handler, leaving the revealed learning card stuck after answer reveal. | The proxy now calls the current underlying Continue button with `click()` after pointer feedback events; browser smoke verifies reveal, Continue advance, TOPIK switching, and review entry loading. |
| BUG-2026-09-02-001 | Fixed | Vocabulary data | TOPIK/KRDict intake could approve non-base headwords such as particles attached to nouns (`밥을`) or spaced phrase fragments, sending them into the learning flow as standalone words. | `scripts/suspect-headword-report.mjs` scans raw vocabulary for particle suffixes, separators, and phrase-like headwords, writes `docs/suspect-headword-report.md`, and moves clear/uncertain published suspects to `needs_review` before `data/vocabulary.json` is regenerated. |
| BUG-2026-09-02-002 | Fixed | Mobile navigation | The mobile bottom `复习` tab could behave like a direct page jump, and tab ordering/labels drifted from the intended `学习 / 多义 / 词库 / 复习` path. | Runtime navigation now normalizes the mobile tabbar label/order, opens a lightweight review submenu on single tap, and only enters the review section on double tap or the explicit `进入复习页` control. |
| BUG-2026-09-02-003 | Fixed | Mobile layout | Mobile learning settings, polysemy library controls, and learned-library actions could wrap into vertical text or overlap when the content width narrowed. | Critical mobile bugfix styles keep TOPIK chips equal, keep learned actions horizontal, split the polysemy library summary into title/count lines, and arrange footer/action controls into compact grids. |
| BUG-2026-09-02-004 | Guarded | Polysemy review | Polysemy review entry points could show a review path even when the user had no matching learned polysemy records, making `같다` appear as a forced default queue item. | Runtime queue guards disable single-sense and whole-word polysemy review entry points when no `memory:*` records exist; the library remains visible for browsing only. |
| BUG-2026-09-02-005 | Fixed | Audio control | Learning/review `听` buttons could appear clickable without invoking speech playback or fallback feedback. | A capture-phase TTS handler plays the current Korean headword or actual pronunciation through `speechSynthesis` and shows a non-blocking toast when playback is unavailable. |
| BUG-2026-09-02-006 | Fixed | Mobile tabbar | After opening the mobile `多义` tab, subsequent bottom-tab taps could leave the page stuck, keep review overlays open, or route unrelated tabs into the polysemy section. | The mobile tabbar now handles click/double-click in one capture-phase controller, closes secondary menus on non-review tabs, jumps to explicit section anchors, and preserves page scrolling on the polysemy section. |
| BUG-2026-09-02-007 | Fixed | Review submenu | The mobile `复习` tab still lacked the requested toggle/double-tap contract and did not expose all review targets in one consistent menu. | Single tap opens the six-item submenu, second tap closes it, double tap enters the main review section, and explicit menu items route to today, full library, rescue, single-sense, whole-word, and sound-rule review targets. |
| BUG-2026-09-02-008 | Fixed | Polysemy queue | Learning/reviewing polysemy could still fall back to a fixed first entry when no real queue existed, making `같다` appear even before the learner had a valid record. | The bundled polysemy selection now uses unlearned senses for learning, real learned/due IDs for review, and leaves review entry points empty/disabled when there is no eligible memory record. |
| BUG-2026-09-02-009 | Fixed | Revealed card data mapping | Revealed-card example blocks could reuse the sense gloss when no real example existed, creating false example content and increasing clipping risk for later blocks. | The visible card shell now renders only real example text from the active sense or original example node, clears gloss-equivalent fallback text, and lets long knowledge content scroll only when it actually overflows above the fixed Continue footer. |
| BUG-2026-09-02-010 | Fixed | Pages regression harness | The Pages UI regression workflow still measured hidden React-owned card nodes and enforced the obsolete single-button Continue width/no-internal-overflow contract. | `scripts/ui-regression.mjs` now targets visible v3 shell controls, measures the three-button outer action rail, and ignores the allowed long-content knowledge scroller while preserving hover-geometry checks. |
| BUG-2026-09-02-011 | Fixed | Revealed card long content | Long revealed learning cards could clip fixed collocations while still hiding the scrollbar, and dictionary-definition rows for entries such as `고등학교` / `교통사고` could appear as examples. | `assets/layout-system-v1a.js` filters definition/gloss-equivalent example text, hides the example block when no real example exists, and confines overflow to the knowledge area above the fixed Continue footer. |
| BUG-2026-09-02-012 | Fixed | Mobile review submenu | Mobile submenu items for single-sense and whole-word polysemy review could open the polysemy page without invoking the React-owned trainer controls. | The submenu now routes through the real polysemy library summary/action buttons so menu clicks enter the requested review mode, while the close control remains centered and visually clickable. |
| BUG-2026-09-02-013 | Fixed | Polysemy answer state | Polysemy learning and review could reuse the previous submitted/result state or repeat the same weak whole-word item after clicking next. | The polysemy next handler now advances within the active queue, clears selected/result state for each new item, and ends the run instead of forcing a stale fallback. |
| BUG-2026-09-02-014 | Fixed | Mobile regression harness | The last mobile fixes could be claimed without reproducing the exact reported words, card states, and main-content review controls. | `artifacts/specific-mobile-regression.cjs` now opens `교통사고`, `고등학교`, and `같다`, checks visible reveal clipping/scroll behavior and real examples, runs 5-item polysemy learn/single/multi paths, and verifies compact mobile header, main review secondary buttons, review-option safe margins, and matching collapsed/expanded polysemy-library radius. |
| BUG-2026-09-04-001 | Fixed | CI smoke alignment | The browser smoke test still tried to interact with the removed mobile learning-rule control, and compact polysemy secondary buttons could fail the fixed-height consistency audit. | `scripts/browser-smoke.mjs` now asserts the learning-rule entry is absent instead of clicking it, and the mobile Layout System locks polysemy secondary buttons to the compact 34px grid height. |
| BUG-2026-09-04-002 | Fixed | UI regression harness | The Pages UI regression test could leave the mouse hovering over the learning action rail and then time out while waiting for the target button to become stable. | `scripts/ui-regression.mjs` now moves the pointer out of the card controls and waits briefly before the reveal click, preserving the real click path while avoiding hover-state test interference. |
